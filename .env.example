DATABASE_URL=your-postgres-connection-string-here
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_JWT_SECRET=your_supabase_jwt_secret
SUPABASE_BUCKET_NAME=your-bucket-name
OPENAI_API_KEY=your-openai-api-key-here

# ──────────────────────────────────────────────────────────────────────────────
# Green Button Utility Data Integration
# ──────────────────────────────────────────────────────────────────────────────

# SDG&E Connect My Data (CMD) - Direct Green Button OAuth
# Obtain credentials from SDG&E developer portal after application approval
SDGE_CMD_CLIENT_ID=your-sdge-cmd-client-id
SDGE_CMD_CLIENT_SECRET=your-sdge-cmd-client-secret
SDGE_CMD_REDIRECT_URI=https://your-app.com/api/utility/callback
# Production Sempra gateway endpoints (defaults if not set)
# SDGE_CMD_BASE_URL=https://soagwx.sempra.com:3443/DataCustodian/espi/1_1/resource
# SDGE_CMD_AUTH_URL=https://soagwx.sempra.com:3443/DataCustodian/oauth/authorize
# SDGE_CMD_TOKEN_URL=https://soagwx.sempra.com:3443/DataCustodian/oauth/token

# UtilityAPI (Fallback Aggregator)
# Sign up at https://utilityapi.com for API access
UTILITYAPI_API_KEY=your-utilityapi-api-key 
UTILITYAPI_BASE_URL=https://utilityapi.com/api/v2
UTILITYAPI_WEBHOOK_SECRET=your-utilityapi-webhook-secret

# Token Encryption
# Generate a secure 32-byte key: python -c "import secrets; print(secrets.token_hex(32))"
TOKEN_ENCRYPTION_KEY=your-32-byte-hex-encryption-key

# ──────────────────────────────────────────────────────────────────────────────
# SFTP File Ingestion — SDG&E VPS watcher
# ──────────────────────────────────────────────────────────────────────────────

# Shared secret between VPS watcher script and backend
# Generate: python -c "import secrets; print(secrets.token_hex(32))"
SFTP_INGEST_SECRET=your-sftp-ingest-shared-secret

# (Optional) S3-compatible storage credentials — only needed if using
# SFTP To Go or similar S3-backed SFTP service instead of direct VPS upload
# SFTPTOGO_S3_ACCESS_KEY_ID=
# SFTPTOGO_S3_SECRET_ACCESS_KEY=
# SFTPTOGO_S3_ENDPOINT=
# SFTPTOGO_S3_BUCKET=

# ──────────────────────────────────────────────────────────────────────────────
# Enphase Solar Monitoring Integration
# ──────────────────────────────────────────────────────────────────────────────

# Enphase Developer API v4 OAuth credentials
# Register at https://developer-v4.enphase.com to obtain credentials
# Required scopes: read (for system discovery and telemetry)
ENPHASE_CLIENT_ID=your-enphase-client-id
ENPHASE_CLIENT_SECRET=your-enphase-client-secret

# Enphase API Key - separate from OAuth credentials
# Found in developer portal under your application's API key
ENPHASE_API_KEY=your-enphase-api-key

# OAuth callback URL - must match what's registered in Enphase developer portal
# Format: https://your-app.com/api/enphase/oauth/callback
ENPHASE_REDIRECT_URI=https://your-app.com/api/enphase/oauth/callback

# ──────────────────────────────────────────────────────────────────────────────
# Homeowner Self-Service Signup
# ──────────────────────────────────────────────────────────────────────────────

# User ID that owns self-service homeowner properties/audits
# Dev:  d18a9cd4-08b8-4123-8c4e-b17c90adaa62
# UAT:  fa5b2103-d3cb-4a18-8f7c-1a97ad749c10
SELF_SERVICE_USER_ID=your-auditor-user-id

# Email address to notify when a new homeowner signs up (optional)
SIGNUP_NOTIFICATION_EMAIL=

# SMTP settings for transactional emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=35rfeeney@gmail.com
SMTP_PASSWORD=Madsen35!

# From address for notification emails (optional, defaults to SMTP_USER)
FROM_EMAIL=
# --- DOE Home Energy Score (HEScore) API ---
# Obtain after DOE Software Partner approval (email assessor@ee.doe.gov).
# Scores require a DOE-approved Qualified Assessor login.
HESCORE_BASE_URL=https://sandbox.hesapi.labworks.org
HESCORE_API_KEY=your-partner-api-key
HESCORE_ASSESSOR_USERNAME=your-assessor-username
HESCORE_ASSESSOR_PASSWORD=your-assessor-password
