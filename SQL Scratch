CREATE TABLE properties (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  addressline1 TEXT NOT NULL,
  sqft INTEGER
);

DROP TABLE properties

CREATE TABLE properties (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  street TEXT,
  city TEXT,
  state TEXT,
  zip_code TEXT,
  sqft INTEGER,
  year_built INTEGER
);
INSERT INTO properties (street, city, state, zip_code, sqft, year_built)
VALUES 
('123 Main St', 'San Diego', 'CA', '92101', 1500, 1985),
('456 Oak Ave', 'Los Angeles', 'CA', '90001', 2000, 1972);

DROP TABLE properties;

delete from properties;
delete from audits;
delete from audit_steps;
delete from audit_media where audit_media.media_type = 'document'
delete from agent_conversations where id > 64

select * from properties;
select * from audits;
select * from audit_steps;
select * from audit_media;
select id, file_name, summary, created_at 
from audit_media 
where audit_id=19 and step_type='exterior';
select * from agent_conversations order by created_at;
This image shows the exterior side of a residential building, including part of the wall, roof, and a metal mesh gate. Observable details relevant to an energy audit:- The exterior wall has a textured stucco finish.- There is visible electrical conduit mounted on the wall.- The roof has rain gutters and a downspout running vertically along the wall. The downspout directs water into a black extension at the base.- There appears to be vegetation and some plant growth at ground level near the foundation.- The area between buildings is open and shows a dirt/grass strip.- The wall’s condition appears intact with no visible cracks or significant deterioration. The window appears to be single- or double-pane but is not clearly visible for further
select * from audits;

select * from audit_media;